
// ============================================================================


exports.TX = function (client)
{//send values to client
    var Tx =
    {
      
    };
    client.emit('RxEvent', JSON.stringify(Tx));
};